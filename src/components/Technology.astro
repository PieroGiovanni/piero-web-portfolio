---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<div class="flex flex-col items-center sm:gap-2 justify-center">
  <div class="flex size-8 sm:size-14">
    <slot />
  </div>
  <p class="text-center text-xs sm:text-base">{name}</p>
</div>
